# Assets Directory

Place graphical assets, icons, and local Tesseract OCR binaries in this directory.

To run with portable Tesseract, copy the `tesseract` OCR bin directory into this folder (or the root directory) under the name `tesseract/`.
